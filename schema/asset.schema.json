{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://windows.design/schemas/expressive-asset.schema.json",
  "title": "Expressive Asset",
  "description": "Metadata contract for a single asset in the Expressive Assets library. Every field here has to earn its place at 500+ assets: identity that never breaks, enough text to make the thing findable, one lifecycle axis, and a trail back to the Figma source.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "name",
    "type",
    "collection",
    "status",
    "themes",
    "sizes",
    "colors",
    "recolorable",
    "keywords",
    "variants",
    "version",
    "updated"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable, globally unique identifier. Format: <collection>.<slug>. Never change this once published \u2014 every consumer references it, and a rename is a breaking change. Change `name` instead and add the old one to `aliases`.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*\\.[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "name": {
      "type": "string",
      "description": "Human-readable display name shown in the browser UI.",
      "minLength": 1
    },
    "aliases": {
      "type": "array",
      "description": "Names this asset used to have, or is commonly called. Search matches against these, so a rename never makes an asset unfindable for the people who learned the old name.",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "keywords": {
      "type": "array",
      "description": "Search terms, especially synonyms someone would type who does not know the asset's name. At 500+ assets this is the difference between a library and a folder of SVGs.",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9 -]+$"
      }
    },
    "description": {
      "type": "string",
      "description": "One line: what the artwork depicts and when to reach for it. Doubles as the duplicate-detector \u2014 two assets with the same description are usually the same idea drawn twice.",
      "maxLength": 280
    },
    "type": {
      "type": "string",
      "description": "Top-level asset kind.",
      "enum": [
        "icon",
        "illustration"
      ]
    },
    "collection": {
      "type": "string",
      "description": "Sub-collection within the type. Extend this list as new collections are added \u2014 it is deliberately open to growth.",
      "enum": [
        "system",
        "product",
        "file",
        "oobe",
        "m365",
        "device",
        "app",
        "third-party",
        "wip"
      ]
    },
    "product": {
      "type": "string",
      "description": "For product artwork only: which product it represents (e.g. \"Outlook\", \"Xbox\"). Absent for systemic artwork.",
      "minLength": 1
    },
    "status": {
      "type": "string",
      "description": "The single lifecycle axis. draft = in progress, do not ship. published = approved for use. deprecated = still present for back-compat, must not be used in new work.",
      "enum": [
        "draft",
        "published",
        "deprecated"
      ]
    },
    "replacedBy": {
      "type": "string",
      "description": "Asset id that supersedes this one. Required when status is deprecated \u2014 a deprecation with no migration target just strands consumers.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*\\.[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "themes": {
      "type": "array",
      "description": "Rendered theme variants that exist on disk. standard is the full-color base; outline and filled are the monochrome reductions. The keys match what the UI shows, so nothing means two things in two places.",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "enum": [
          "standard",
          "outline",
          "filled"
        ]
      }
    },
    "sizes": {
      "type": "array",
      "description": "Pixel sizes the artwork is designed to read correctly at \u2014 not the sizes it can technically scale to. If it turns to mud at 12px, do not list 12.",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "integer",
        "minimum": 8,
        "maximum": 512
      }
    },
    "colors": {
      "type": "object",
      "description": "Brand colors baked into the standard variant.",
      "additionalProperties": false,
      "required": [
        "primary",
        "secondary"
      ],
      "properties": {
        "primary": {
          "type": "string",
          "pattern": "^#[0-9A-F]{6}$"
        },
        "secondary": {
          "type": "string",
          "pattern": "^#[0-9A-F]{6}$"
        }
      }
    },
    "recolorable": {
      "type": "boolean",
      "description": "Whether the artwork may be retinted to a Windows accent. Set false for trademarked brand marks that must ship in their own colors \u2014 the browser disables the accent picker for those, so nobody ships a magenta Excel icon."
    },
    "variants": {
      "type": "object",
      "description": "Artwork paths, keyed by theme and then by the pixel size the drawing was made for. Windows product icons are REDRAWN per size \u2014 the 16px Excel icon is a different drawing from the 48px one, not a scaled copy \u2014 so each is stored separately. Artwork that is genuinely size-agnostic (a single scalable drawing) uses the one key \"any\". Top-level keys must match the themes array.",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "minProperties": 1,
        "propertyNames": {
          "pattern": "^(any|[0-9]{1,3})$"
        },
        "additionalProperties": {
          "type": "string",
          "pattern": "^assets/.+\\.svg$"
        }
      }
    },
    "figma": {
      "type": "object",
      "description": "Where this artwork came from upstream. At 500+ assets, being able to jump from a file on disk back to the component that produced it is not optional.",
      "additionalProperties": false,
      "required": [
        "fileKey",
        "nodeId"
      ],
      "properties": {
        "fileKey": {
          "type": "string",
          "minLength": 1
        },
        "nodeId": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "source": {
      "type": "object",
      "description": "Upstream provenance for artwork the library did not draw. Required in practice for anything vendored from a third-party project \u2014 the license obligation travels with the file, not with the README, and an asset that cannot name where it came from cannot be audited later.",
      "additionalProperties": false,
      "required": [
        "project",
        "license"
      ],
      "properties": {
        "project": {
          "type": "string",
          "minLength": 1
        },
        "url": {
          "type": "string",
          "minLength": 1
        },
        "license": {
          "type": "string",
          "minLength": 1
        },
        "copyright": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "owner": {
      "type": "string",
      "description": "GitHub handle or team responsible for this asset."
    },
    "version": {
      "type": "string",
      "description": "Semver for the artwork itself. Minor for a redraw, major for a shape change that breaks visual continuity.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "updated": {
      "type": "string",
      "description": "ISO date of the last artwork change.",
      "format": "date",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "notes": {
      "type": "string",
      "description": "Usage guidance, restrictions, or redraw history."
    },
    "generated": {
      "type": "array",
      "description": "Cells in the style-by-size matrix that were produced here rather than received, as \"style:size\" strings. A generated cell is one of: the same drawing shown at another size, a style derived from the received artwork by luminance, or a silhouette. It exists so the matrix is square and the gap stays visible; it is not a substitute for a drawing, and a published asset can still carry generated cells. Absent means every variant listed is artwork as it arrived.",
      "items": {
        "type": "string",
        "pattern": "^(standard|outline|filled):(any|[0-9]{1,3})$"
      },
      "uniqueItems": true
    },
    "roles": {
      "type": "object",
      "description": "Derived color addresses, one per distinct fill. Frozen once written: `hash` covers the paint they were derived from, and scripts/derive-roles.mjs --check fails rather than renumbering when the artwork moves.",
      "additionalProperties": false,
      "required": [
        "from",
        "derivedBy",
        "k",
        "groups",
        "archetypes",
        "shapes",
        "hash"
      ],
      "properties": {
        "from": {
          "type": "string",
          "description": "The variant the roles were derived from, normally the largest Standard."
        },
        "derivedBy": {
          "type": "string",
          "description": "Clustering space and the criterion used to choose the group count."
        },
        "k": {
          "type": "integer",
          "minimum": 0,
          "maximum": 4,
          "description": "Number of color groups. 0 means every fill is held; under 2 means a two-color theme needs the part axis to bind to this asset."
        },
        "groups": {
          "type": "array",
          "description": "The relative address. Groups are ordered most distinct first, so a theme handing out colors in order keeps the separation the artwork had and folds the already-similar groups together at the tail.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "id",
              "basecolor",
              "members"
            ],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^color[0-9]+$"
              },
              "basecolor": {
                "type": "boolean",
                "description": "Intentional white or black. Addressable, but not themed unless a theme asks for it."
              },
              "spread": {
                "type": "number",
                "description": "Mean chromaticity distance from this group to every other. Absent on a basecolor group."
              },
              "members": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "id",
                    "stops",
                    "shapes"
                  ],
                  "properties": {
                    "id": {
                      "type": "string",
                      "pattern": "^color[0-9]+\\.(basecolor\\.)?part[0-9]+$"
                    },
                    "stops": {
                      "type": "array",
                      "minItems": 1,
                      "items": {
                        "type": "string",
                        "pattern": "^#[0-9a-f]{6}$"
                      },
                      "description": "The fill this member is, in stop order. One entry is a flat fill; more is a gradient ramp."
                    },
                    "shapes": {
                      "type": "array",
                      "minItems": 1,
                      "items": {
                        "type": "integer",
                        "minimum": 0
                      },
                      "description": "Indices of the painted shapes carrying this fill, in document order."
                    },
                    "alsoFills": {
                      "type": "array",
                      "minItems": 1,
                      "description": "Other fills merged into this member as near-identical. Kept so a shape carrying one can still be addressed; without it a merged fill has no written-down signature and no theme can reach it.",
                      "items": {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "type": "string",
                          "pattern": "^#[0-9a-f]{6}$"
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "shapes": {
          "type": "array",
          "description": "Both addresses for every painted shape. `part` is a structural name, is not part of either scheme, and stays null.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "i",
              "role",
              "archetype",
              "part"
            ],
            "properties": {
              "i": {
                "type": "integer",
                "minimum": 0
              },
              "role": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "archetype": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "part": {
                "type": [
                  "string",
                  "null"
                ]
              }
            }
          }
        },
        "hash": {
          "type": "string",
          "pattern": "^[0-9a-f]{16}$"
        },
        "archetypes": {
          "type": "array",
          "description": "The absolute address. The same fill answers to the same name in every asset in the library, so a theme can select every green without opening each file.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "id",
              "members"
            ],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^(light-|deep-|muted-|muted-deep-)?[a-z]+$"
              },
              "members": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "id",
                    "stops",
                    "shapes"
                  ],
                  "properties": {
                    "id": {
                      "type": "string",
                      "pattern": "^[a-z-]+\\.part[0-9]+$"
                    },
                    "stops": {
                      "type": "array",
                      "minItems": 1,
                      "items": {
                        "type": "string",
                        "pattern": "^#[0-9a-f]{6}$"
                      },
                      "description": "The fill this member is, in stop order. One entry is a flat fill; more is a gradient ramp."
                    },
                    "shapes": {
                      "type": "array",
                      "minItems": 1,
                      "items": {
                        "type": "integer",
                        "minimum": 0
                      },
                      "description": "Indices of the painted shapes carrying this fill, in document order."
                    },
                    "alsoFills": {
                      "type": "array",
                      "minItems": 1,
                      "description": "Other fills merged into this member as near-identical. Kept so a shape carrying one can still be addressed; without it a merged fill has no written-down signature and no theme can reach it.",
                      "items": {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "type": "string",
                          "pattern": "^#[0-9a-f]{6}$"
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
